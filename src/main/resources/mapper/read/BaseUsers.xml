<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.base.vistter.dao.ReadDataDao">

    <select id="PERMISSION_USERS_PAGERData" parameterType="map" resultType="map">
        select bu.* , br.NAME AS ROLENAME from base_users bu
        left join base_role br on bu.BASE_ROLE_ID = br.ID
        where bu.PROJECT_CODE = #{PROJECT_CODE}
        <if test="USERNAME!= null and USERNAME != ''">
            and bu.USERNAME  = #{USERNAME}
        </if>
        <if test="TELPHONE!= null and TELPHONE != ''">
            and bu.TELPHONE  = #{TELPHONE}
        </if>
        <if test="STATUS!= null and STATUS != ''">
            and bu.STATUS  = #{STATUS}
        </if>
      order by bu.MODIFIER desc limit #{start} , #{limit}
    </select>

    <select id="PERMISSION_USERS_PAGERCount" parameterType="map" resultType="long">
        select count(*) from base_users bu
        where bu.PROJECT_CODE = #{PROJECT_CODE}
        <if test="USERNAME!= null and USERNAME != ''">
            and bu.USERNAME  = #{USERNAME}
        </if>
        <if test="TELPHONE!= null and TELPHONE != ''">
            and bu.TELPHONE  = #{TELPHONE}
        </if>
        <if test="STATUS!= null and STATUS != ''">
            and bu.STATUS  = #{STATUS}
        </if>
    </select>

    <select id="PERMISSION_USERS_LOADSingle" parameterType="map" resultType="map">
        select * from base_users where ID = #{ID}
    </select>

    <select id="PERMISSION_USER_COUNT_PROJECTID" parameterType="map" resultType="long">
      select count(*) from base_users where
              PROJECT_CODE = (select PROJECT_CODE from base_project where id = #{ID})
    </select>

    <select id="PERMISSION_USER_COUNT_PROJECTIDS" parameterType="map" resultType="long">
        select count(*) from base_users where PROJECT_CODE IN
            (select PROJECT_CODE from base_project where id in
                <foreach collection="IDS" item="item" open="(" separator="," close=")">
                    #{item}
                </foreach>
              )
    </select>

    <select id="PERMISSION_USER_COUNT_ROLEID" parameterType="map" resultType="long">
        select count(*) from base_users where BASE_ROLE_ID = #{ID}
    </select>

    <select id="PERMISSION_USER_COUNT_ROLEIDS" parameterType="map" resultType="long">
        select count(*) from base_users where BASE_ROLE_ID IN
        <foreach collection="IDS" item="item" open="(" separator="," close=")">
            #{item}
        </foreach>
    </select>

    <select id="PERMISSION_USERS_COUNT_USERNAME" parameterType="map" resultType="long">
        select count(*) from base_users where PROJECT_CODE = #{PROJECT_CODE} and USERNAME = #{USERNAME}
    </select>

</mapper>
