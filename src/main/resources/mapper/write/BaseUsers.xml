<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.base.vistter.dao.WriteDataDao">

    <insert id="PERMISSION_USERS_SAVE" parameterType="map">
        insert into base_users (ID, USERNAME, TELPHONE, PASSWORD, BASE_ROLE_ID, STATUS, PROJECT_CODE, CREATEOR,
                                CREATE_DATE, MODIFIER, MODIFY_DATE)
        values (uuid(), #{USERNAME}, #{TELPHONE}, #{PASSWORD}, #{BASE_ROLE_ID}, #{STATUS}, #{PROJECT_CODE}, #{CREATEOR},
                now(), #{MODIFIER}, now())
    </insert>

    <update id="PERMISSION_USERS_UPDATE" parameterType="map">
        update base_users
        set TELPHONE     = #{TELPHONE},
            PASSWORD     = #{PASSWORD},
            BASE_ROLE_ID = #{BASE_ROLE_ID},
            STATUS       = #{STATUS},
            MODIFIER     = #{MODIFIER},
            MODIFY_DATE  = now()
        where ID = #{ID}
    </update>

    <delete id="PERMISSION_USERS_DELETEBYID" parameterType="map">
        DELETE
        FROM base_users
        WHERE ID = #{ID}
    </delete>

    <delete id="PERMISSION_USERS_DELETE" parameterType="map">
        DELETE
        FROM base_users
        WHERE ID in
        <foreach collection="IDS" item="item" open="(" separator="," close=")">
            #{item}
        </foreach>
    </delete>

    <update id="PERMISSION_USERS_START" parameterType="map">
        UPDATE base_users
        SET STATUS      = 0,
            MODIFY_DATE=now()
        WHERE ID = #{ID}
    </update>

    <update id="PERMISSION_USERS_STOP" parameterType="map">
        UPDATE base_users
        SET STATUS      = 1,
            MODIFY_DATE=now()
        WHERE ID = #{ID}
    </update>

    <update id="PERMISSION_USERS_RESETPWD" parameterType="map">
        UPDATE base_users
        set PASSWORD = #{PASSWORD}
        where ID = #{ID}
    </update>
</mapper>
