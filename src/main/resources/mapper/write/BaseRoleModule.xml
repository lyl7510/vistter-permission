<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.base.vistter.dao.WriteDataDao">
    <insert id="PERMISSION_ROLEMODULE_SAVE" parameterType="map">
            insert into base_role_module (ID ,BASE_ROLE_ID ,
            BASE_MODULE_ID,PROJECT_CODE,CREATEOR,CREATE_DATE,MODIFIER,MODIFY_DATE)
            values
            <foreach collection="MODULE_IDS" item="item" index="index" separator=",">
                (
                uuid(),
                #{BASE_ROLE_ID},
                #{item},
                #{PROJECT_CODE},
                #{CREATEOR},
                now(),
                #{MODIFIER},
                now()
                )
            </foreach>
    </insert>

    <delete id="PERMISSION_ROLEMODULE_DELETE" parameterType="map">
        delete
            base_role_module from base_role_module
         INNER JOIN base_module on base_role_module.BASE_MODULE_ID = base_module.ID
        where base_role_module.BASE_ROLE_ID = #{BASE_ROLE_ID}
          and base_module.MODULE_TYPE = #{MODULE_TYPE}
    </delete>

    <delete id="PERMISSION_ROLEMODULE_DELETEBYMODULEID" parameterType="map">
        delete from base_role_module where BASE_MODULE_ID = #{ID}
    </delete>

    <delete id="PERMISSION_ROLEMODULE_DELETEBYMODULEIDS" parameterType="map">
        delete from base_role_module where BASE_MODULE_ID in
        <foreach collection="IDS" item="item" open="(" separator="," close=")">
            #{item}
        </foreach>
    </delete>

    <delete id="PERMISSION_ROLEMODULE_DELETEBYID" parameterType="map">
        delete from base_role_module where BASE_ROLE_ID = #{ID}
    </delete>

    <delete id="PERMISSION_ROLEMODULE_DELETE_ROLEIDS" parameterType="map">
        delete from base_role_module where BASE_ROLE_ID in
        <foreach collection="IDS" item="item" open="(" separator="," close=")">
            #{item}
        </foreach>
    </delete>
</mapper>
