<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.base.vistter.dao.WriteDataDao">

    <insert id="PERMISSION_ROLE_SAVE" parameterType="map">
        INSERT INTO base_role (ID, PROJECT_CODE, NAME, STATE, CREATEOR, CREATE_DATE, MODIFIER, MODIFY_DATE)
        values (uuid(), #{PROJECT_CODE}, #{NAME}, #{STATE}, #{CREATEOR}, NOW(), #{MODIFIER}, now())
    </insert>

    <update id="PERMISSION_ROLE_UPDATE" parameterType="map">
        update base_role
        set NAME        = #{NAME},
            STATE       = #{STATE},
            MODIFIER    = #{MODIFIER},
            MODIFY_DATE = NOW()
        where ID = #{ID}
    </update>

    <delete id="PERMISSION_ROLE_DELETEBYID" parameterType="map">
        DELETE
        FROM base_role
        WHERE ID = #{ID}
    </delete>

    <update id="PERMISSION_ROLE_DELETE" parameterType="map">
        DELETE
        FROM base_role
        WHERE ID in
        <foreach collection="IDS" item="item" open="(" separator="," close=")">
            #{item}
        </foreach>
    </update>

    <update id="PERMISSION_ROLE_START" parameterType="map">
        UPDATE base_role
        SET STATE     = 0,
            MODIFY_DATE=now()
        WHERE ID = #{ID}
    </update>

    <update id="PERMISSION_ROLE_STOP" parameterType="map">
        UPDATE base_role
        SET STATE     = 1,
            MODIFY_DATE=now()
        WHERE ID = #{ID}
    </update>
</mapper>
