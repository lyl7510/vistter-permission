<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.base.vistter.dao.WriteDataDao">

    <insert id="PERMISSION_PROJECT_SAVE" parameterType="map">
        INSERT INTO base_project (ID, PROJECT_CODE, PROJECT_NAME, PROJECT_DES, TELPHONE_LOGIN, SECURITY_CODE, STATUS,
                                  MODIFY_DATE)
        values (uuid(), #{PROJECT_CODE}, #{PROJECT_NAME}, #{PROJECT_DES}, #{TELPHONE_LOGIN}, #{SECURITY_CODE},
                #{STATUS}, now())
    </insert>

    <update id="PERMISSION_PROJECT_UPDATE" parameterType="map">
        UPDATE base_project
        SET PROJECT_NAME  = #{PROJECT_NAME},
            PROJECT_DES=#{PROJECT_DES},
            TELPHONE_LOGIN=#{TELPHONE_LOGIN},
            SECURITY_CODE=#{SECURITY_CODE},
            STATUS=#{STATUS},
            MODIFY_DATE=NOW()
        WHERE ID = #{ID}
    </update>

    <delete id="PERMISSION_PROJECT_DELETEBYID" parameterType="map">
        DELETE
        FROM base_project
        WHERE ID = #{ID}
    </delete>

    <delete id="PERMISSION_PROJECT_DELETE" parameterType="map">
        DELETE
        FROM base_project
        WHERE ID in
        <foreach collection="IDS" item="item" open="(" separator="," close=")">
            #{item}
        </foreach>
    </delete>

    <update id="PERMISSION_PROJECT_START" parameterType="map">
        UPDATE base_project
        SET STATUS     = 0,
            MODIFY_DATE=now()
        WHERE ID = #{ID}
    </update>

    <update id="PERMISSION_PROJECT_STOP" parameterType="map">
        UPDATE base_project
        SET STATUS     = 1,
            MODIFY_DATE=now()
        WHERE ID = #{ID}
    </update>

</mapper>
